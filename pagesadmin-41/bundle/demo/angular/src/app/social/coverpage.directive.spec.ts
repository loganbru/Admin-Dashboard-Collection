import { CoverpageDirective } from './coverpage.directive';

describe('CoverpageDirective', () => {
  it('should create an instance', () => {
    const directive = new CoverpageDirective();
    expect(directive).toBeTruthy();
  });
});
