# Bulkit landing kit 2

Landing kit 2 Package

## Demo file

The index file is named `landing-v1.html` in the main demo

## Get started

* Open a terminal
* Change directory to the root folder of this package
* run `npm install`
* run `gulp build`
* run `gulp default`